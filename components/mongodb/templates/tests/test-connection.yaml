apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "mongodb-test.fullname" . }}-test-connection"
  labels:
    {{- include "mongodb-test.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "mongodb-test.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
